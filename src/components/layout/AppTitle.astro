---
import { SITE_NAME } from '../../utils/meta';

const { class: className } = Astro.props;
---

<h1
	class:list={[
		'from-primary to-base-content bg-linear-to-r bg-clip-text text-xl font-bold text-transparent sm:text-2xl',
		className,
	]}
>
	<slot>{SITE_NAME}</slot>
</h1>
